import React from 'react';

/**
 * Project Invite Dialog
 * 이 페이지는 현재 선택된 프로젝트에 사용자를 초대하기 위한 Dialog 입니다.
 * 이 Dialog는 아래 두 가지 경우에 사용됩니다.
 * 1. Researcher 가 PI를 초대할 때
 * 2. PI가 리뷰어를 초대할 떄
 * 초대자의 역할에 따라서 초대자 리스트는 PI, 리뷰어로 구분되어야 합니다.
 * 또한 초대에 사용하는 API도 달라져야 합니다.
 * @constructor
 */
const ProjectInviteDialog = () => {
  const onApproveButtonClick = () => {};
  const loadApproveData = () => {};

  return <></>;
};

export default ProjectInviteDialog